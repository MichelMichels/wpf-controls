<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MichelMichels.Wpf.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MichelMichels.Wpf.Controls;component/Resources/Brushes.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MichelMichels.Wpf.Controls;component/Resources/Measurements.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="WindowControlsTemplate" TargetType="{x:Type local:WindowControls}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <local:MinimizeButton Grid.Column="0" />
            <local:MaximizeButton Grid.Column="1" />
            <local:RestoreButton Grid.Column="1" />
            <local:CloseButton Grid.Column="2" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="WindowControls.Default" TargetType="{x:Type local:WindowControls}">
        <Setter Property="Template" Value="{StaticResource WindowControlsTemplate}" />
        <Setter Property="MinHeight" Value="32" />
    </Style>
</ResourceDictionary>